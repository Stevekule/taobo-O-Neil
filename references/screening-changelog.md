# 迭代更新记录

> 此文件记录「陶博士选股系统自我审查与迭代模块」的所有变更历史
> 核心条件（C1-C18）在任何情况下不可修改

---

## 版本历史

### v1.0 — 2026-08-05
- **类型**：初始创建
- **内容**：
  - 建立双重权重规则体系（核心权重1.0 + 辅助权重0.8）
  - 录入陶博士2006 核心选股条件18条（C1-C18）
  - 录入 TheCrowd2018 + 博士之路2018 辅助条件8条（A1-B5）
  - 建立每周复盘模板与结构化输出格式
  - 定义迭代审批流程与权限边界
- **用户审批**：初始版本，已确认
- **核心条件声明**：C1-C18 未做任何修改，直接来源于陶博士2006公开文章

### v1.1 — 2026-08-05
- **类型**：新增功能（辅助层扩展）
- **内容**：
  - 新增「每日筛选工作流」：每个交易日 16:00 自动/手动触发分层筛选
  - 新增 `references/daily-screening-template.md`：每日筛选结构化输出模板
  - 包含筛选漏斗概览、大盘环境判断、候选标的明细（所有名称须带代码）、TOP5重点关注、板块分布统计
  - 创建自动化定时任务 `automation-1785894075168`：每交易日 16:00 自动执行
  - 更新 SKILL.md 触发条件、工作流章节、输出要求
- **用户审批**：已批准
- **核心条件声明**：C1-C18 未做任何修改

### v1.2 — 2026-08-05
- **类型**：补全定时任务
- **内容**：
  - 创建自动化定时任务 `automation-1785894292694`：每周五 20:00 自动执行每周复盘工作流
  - 至此「每日筛选(周一至周五16:00) + 每周复盘(周五20:00)」定时闭环完成
- **用户审批**：已批准
- **核心条件声明**：C1-C18 未做任何修改

### v1.3 — 2026-08-05
- **类型**：核心层扩展 + 辅助层扩展（用户审批）
- **内容**：
  - 核心条件新增 5 条（C19-C23）：当季 EPS≥25%(C19)、年度 EPS 连续3年复合≥20%(C20，用户拍板放宽自 25%)、PEG≤1.0(C21)、净利润断层历史回溯(C22)、细分行业龙头前2/营收排名(C23)
  - C22 明确为「仅历史回溯检查」，因未来公告不可预知；验证「基本面+技术面共振」
  - 辅助条件新增 3 条（B6-B8）：业绩预增机构持仓股池(B6)、股池排除规则(B7)、量能平台要求(B8)
  - B1(板块强度优先)/B2(横纵经纬法) 已覆盖博士之路2018 框架，B6-B8 聚焦其「基本面把关股池」新维度
  - 同步更新 SKILL.md：条件计数 18→23 条 / 7→8 层；漏斗加载范围 C1-C23 + A1-B8；迭代边界 C1-C23 不可修改
- **用户审批**：博士之路2018 观点全套入档（B6-B8）；陶博士 C19-C23 全套采纳、C20 放宽至 20%
- **核心条件声明**：C1-C23 均来源于陶博士2006 公开文章；C20 阈值 20% 为用户明确决策值（陶博士原文理想 25%-30%）

### v1.4 — 2026-08-05（首跑验证后重构 · 用户审批）
- **类型**：工作流重构（数据入口 + RPS 执行层）
- **背景**：首跑验证发现 neodata 不支持「全市场多条件交叉筛选」、无 RPS/相对强度字段，原 Step 2/Step 3 不可执行（详见 `deliverables/taobo-daily/first-run-validation-2026-08-05.md`）
- **内容**：
  - **入口改为种子池**（Step 2.1）：用 neodata 可得强势股源（板块龙头 / 一年新高 / 涨跌幅排行 / 基金重仓）构建 ≤80 只种子池，绕开不可行的全市场 C1∩C2 交集
  - **RPS 层代理**（Step 3 第2层）：C4-C6 锚定定义不变，执行层以「一年新高/接近新高 + 阶段涨幅排名 + 强弱表现」代理，标注「RPS代理」
  - **C1 口径**：基金持股取"主动偏股基金持仓持股比例"（非全基金，注明差异）
  - **C2 口径**：北向市值 = 沪深港通个券持股数量 × 最新价 自算
  - `taobo-core-conditions.md` 第2层加执行代理注记；`SKILL.md` 数据获取段移除"RPS 相关计算原始数据"并加限制说明
- **用户审批**：种子池 + RPS 代理方案（推荐项）
- **核心条件声明**：C1-C23 锚定定义未改；C4-C6 仅执行层替换为代理，定义保留

### v1.4.1 — 2026-08-05（用户指令修正 · C1 取数通道）
- **类型**：Bug 修正（数据通道）
- **背景**：首跑实跑（盘中数据）发现 v1.4 的「基金重仓资产」NL 种子源**失灵**——查询"基金重仓资产/主动偏股基金重仓持股前50"返回科创50指数行情或空结果，非个股权池；同时 v1.4 的 C1"主动偏股基金持仓持股比例"口径偏窄、且部分标的无此字段。
- **内容**：
  - **移除** Step 2.1「基金重仓持仓：neodata 查询「基金重仓资产」」种子源，标注失灵、不可作种子
  - **C1 改通道**（用户明确指令"去用每季度基金披露的持仓来比对"）：对每只种子查询「股东与机构持仓」报告，取 `机构持仓统计` 中的**公募基金持股数量/市值**（= 每季度基金披露持仓，报告期如 20260630，存在季末时滞），占流通股比例自算，阈值 ≥3%
  - 此口径即陶博士原版"全基金口径"，覆盖完整、数据可靠（盘中实跑验证可用：中微27.9%/中船特气11.6%/卓易信息5.3%/中国巨石12.3%）。**口径核实**：陶博士通达信公式 `FINVALUE(255)`（基金持股占流通股比例）= 每季度披露全部公募基金，非"主动偏股"窄口径；其原文以"基金持股 vs 保险/社保"对比确认池子=公募基金全口径（来源：陶博士2006《月线反转2.0版本股票池》20181006 + 《基金持股和北向持股的量化选股方法》20190102）。故 v1.4 曾用的"主动偏股基金持仓持股比例"不符陶博士，v1.4.1 修正为全基金正确。
  - 漏斗第1层说明同步由"主动偏股基金口径"改为"全基金口径：公募基金持股，季度披露"
- **用户审批**：用户 2026-08-05 口头指令「基金重仓资产这块的查询，去用每季度基金披露的持仓来比对」
- **核心条件声明**：C1 锚定定义（基金持股≥3%）未改，仅取数通道与口径修正；C1-C23 锚定定义全部未改

### v1.4.2 — 2026-08-05（对账修正 · C9 月线反转6.2 公式失真补正）
- **类型**：Bug 修正（核心条件转录失真）
- **背景**：用户要求"将当前 skill 选股条件与陶博士量化选股方法做比对"。逐条核对陶博士2006 原文（《月线反转6.2版本》2022-10-21 完整公式源码）后，发现 C9 两处明显失真：
  1. **FYX1 RPS 逻辑写反**：原蒸馏写"RPS50 ≥ 87 且 RPS120 ≥ 90"（AND），陶博士原文为"RPS50 > 87 **或** RPS120 ≥ 90"（OR，满足一组即可）。AND 使月线反转信号显著变严、漏掉大量有效标的。
  2. **FYX6 整条缺失**：6.2 相较 5.0 新增的"从阶段最低点上涨幅度限制"（HHV(H,30)/LLV(L,120)<1.50~1.75，上限50%~75%，超则易失败），原蒸馏版完全未收录。
- **内容**：按陶博士原文七条件（FYX1-FYX7）重写 C9，修正 FYX1 为 OR、补回 FYX6，标注源码出处；FYX2-FYX5/FYX7 经核对与原文一致仅措辞对齐。
- **比对其余 C 条件结论**：
  - 一致/已对齐：C1(全基金口径)、C2(3000万原值,后提5000-6000万已注)、C3、C4-C6、C8、C10-C11、C13-C18、C19/C21/C23
  - 有意偏差(用户决策)：C20 年度复合20%(陶博士理想25-30%,v1.3放宽)
  - 轻微偏差：C12 口袋支点 RPS≥95(陶博士一般≥90,偏严)
  - 用法适配(非错误)：C22 净利润断层——陶博士作买点/共振信号,skill 作"历史回溯检查"
  - 待核实：C7 港资增仓量化阈值(陶博士 exact 数值未公开核实)
- **边界说明**：A1-B8 来自 TheCrowd2018/博士之路2018，非陶博士2006，不纳入与陶博士比对
- **核心条件声明**：本次为修正转录失真以还原原文，非迭代改锚定定义；C1-C23 锚定定义未改
- **用户决策执行（对账收口 · 2026-08-05）**：① C12 口袋支点 RPS 由 ≥95 对齐为 ≥90（陶博士一般≥90）；② C20 年度复合增速维持 20% 不变（用户放宽值保留）；③ C7 港资增仓核实后对齐陶博士原文：占流通比增加≥1% 或 市值增加≥1亿（参数 N=1/M=1），原 0.5%/5000万 偏松已修正。C7 出处：陶博士2006《条件选股公式：港资增仓且接近一年新高》`AA1:=市值增加1亿 OR 占流通比增加1%`

### v1.4.3 — 2026-08-06（用户指令新增 · 第0层前置过滤）
- **类型**：核心层扩展（新增，非修改既有）
- **背景**：用户要求"比对 skill 与陶博士文章，看是否含上市一年以上/非ST 第一层过滤"。核对结论：Skill 原"第1层"实为资金面硬门槛（C1-C3），与陶博士真正的"第一层过滤"（RPS 计算前的全市场范围前置过滤）概念错配；全 skill 无"上市一年以上/非ST"显式条件（详见 `deliverables/taobo-daily/taobo-filter-layer-check-2026-08-06.md`）。
- **内容**：在 `taobo-core-conditions.md` 顶部新增「第0层：全市场范围前置过滤」（锚定标准，权重1.0，不可修改）：
  - **C0-1 上市一年以上**：上市交易日数 > 250（`BARSCOUNT(C)>250`），剔除次新股，避免其 IPO 连续涨停扭曲强度排名；次新股特殊通道须走"开板后涨幅大+形态完美"并标注。
  - **C0-2 非ST**：剔除 ST/*ST/退市风险警示，陶博士实盘从不持有。
  - 同步更新 `SKILL.md`：条件计数 23→25 条（C0-C23）、8→9 层（含第0层）；漏斗加载与 Step 3 第0层先行。
- **回测影响验证**：在 546 只回测池上实跑验证——上市一年以上移除 0 只（池内最少 K 线 368 根，均 >250）；非ST 移除 10 只 ST 票（000609/002082/002759/300096/300167/600289/600302/600678/603828/688270）。净保留 536 只，原 683 信号中剔除来自 ST 票的部分后重算 2025 收益率。
- **用户审批**：用户 2026-08-06 口头指令「新增补丁，之后再次更新回测结果」
- **核心条件声明**：新增 C0-1~C0-2，C1-C23 锚定定义未改；此为补全陶博士原版第一道门槛，非修改既有

### v1.4.4 — 2026-08-06（用户通达信原版代码核对 · C1/C2/C7 取数对齐 + 数据源改通达信优先）
- **类型**：核心条件取数对齐 + 数据源优先级调整 + 缺失处置修正
- **背景**：用户提交陶博士2006 通达信原版选股公式代码（港资持股主图指标 / 北向≥3000万 / 基金≥3% / 港资增仓且接近一年新高），要求 skill 条件与通达信原版逐条对齐；并指令"数据源尽量选择通达信"。
- **核对结论（6 点差异 → 逐项修正）**：
  1. **C1 分母函数**：原 skill 公式实为 `FINVALUE(255)/FINVALUE(239)`（已正确），此前对话中一度误读为 `FINANCE(239)`，实测文件无误——**差异不成立**，仅补充"分母须为财务口径同期 `FINVALUE(239)`、非即时 `FINANCE(239)`"说明，防财报期送转股偏移。
  2. **C7 参数（关键修正）**：用户通达信原版参数缺省 **N=0.50% / M=0.50亿 / D=30**（截图公式 `占流通比增加1%:=...>=N` 中 N=0.50、`市值增加1个亿:=...>M*100000000` 中 M=0.50、D=30.00）。⚠️ **v1.4.2（CHANGELOG 第88行）将 C7 对齐为 N=1/M=1 系误读原版，本次纠正回 0.5/0.5/D=30**。门槛由此放宽一倍（原版更松）。
  3. **C7 取数映射**：`GPJYVALUE(6,1,1)` ↔ tdx-connector「十大流通股东·香港中央结算 HKSCC」持股数（全市场通用，含创业板）。
  4. **C2 取数路径（实现缺陷修正）**：原 skill 走"沪深港通个券持股"API，对创业板返回空（假缺失）；修正为优先 tdx HKSCC 字段（对所有 A 股通用），市值=持股数×tdx_quotes 收盘价。
  5. **C1 缺失自算（实现缺陷修正）**：若数据源返回公募持股绝对数但缺占比分母，须用流通A股总数自算占比后再判 ≥3%，不得直接判通过/不通过。
  6. **删除"强势推定"（逻辑错误修正）**：旧版日报表将"数据缺失"当 C1∪C2 通过（8只假通过）。修正为缺失分三类回退：① 不适用(科创板不在沪股通) ② 字段缺失可自算 ③ 取数漏抓须 HKSCC 兜底；统一标「人工复核」，不参与自动通过计数。
- **数据源优先级（用户指令：通达信优先）**：`SKILL.md`「数据获取」章节由"所有数据通过 neodata"改为"资金面 C1/C2/C7 **优先 tdx-connector**，neodata 降级兜底；K线/财务/板块二者均可，优先 tdx"。落实 C1/C2/C7 的 tdx 取数映射表 + 缺失处置铁律。
- **用户审批**：用户 2026-08-06 确认"确认修改，数据源尽量选择通达信"。
- **核心条件声明**：C1(基金≥3%)、C2(北向≥3000万) 锚定定义未改，仅取数通道/口径对齐；C7 参数由 v1.4.2 的误读值(1/1)纠正回用户原版(0.5/0.5/D=30)；此为还原用户通达信原版，非自由迭代。

### v1.4.5 — 2026-08-06（取数通路重跑验证 · 用户指令「重跑一次今天的日报，重点验证取数通路」）
- **类型**：取数通路验证 + 文档修正（非锚定定义变更）
- **背景**：重跑 8/6 日报，逐只对原 v1 报表面板中 8 只「C1/C2 数据缺失」的科创板标的做 tdx 取数通路验证，确认是假缺失(false negative)还是真缺口。
- **验证结论（8/8 全部真实 PASS，0 真缺失）**：
  - **C2 北向市值**：日频 `bszj` 北向接口全市场故障（ErrorInfo -1005）导致原报「缺失」。正确路径 = `ltgd` 的 **HKSCC（香港中央结算）** 字段持股数 × 当日收盘(`HQInfo.Now`) 推算市值。11 只覆盖标的结果：9 PASS / 2 N/A（欧莱 688530、恒坤 688727 真非港股通，C2 不适用，转看 C1 均 PASS）。
  - **C1 基金持股**：原报用 `jgcg` 机构总持股占比作 C1 判据 → 对半导体股因大基金计入严重高估（有研硅机构总占比 79.67% vs 基金仅 ~1.9%），且部分标的不返回字段致「缺失」。正确路径 = `ltgd` **股东类别=「基金」** 持股数之和 ÷ 实际流通A股（最新报告期）。8 只科创板实测：688549 中巨芯 7.34%、688702 盛科 6.01%、688727 恒坤 4.15%、688530 欧莱 3.94% 明确 PASS；688432 有研硅 1.91% / 688596 正帆 2.08% / 688300 联瑞 2.52% / 688503 聚和 2.58% 为**前十大截断下界**（top-10 外中小基金未计入），该 4 只已凭 C2 明确过并集。
  - **关键修正（已固化到 `taobo-core-conditions.md`）**：
    1. ❌ 禁用 `jgcg` 作 C1 判据（机构总占比≠基金占比）；✅ 用 `ltgd`「基金」类别求和。
    2. ❌ 禁用日频 `bszj` 北向接口（全市场故障）；✅ 用 `ltgd` HKSCC 字段兜底。
    3. ⚠️ 市值收盘价取 `HQInfo.Now`（当日收盘）非 `Close`（昨收）。
    4. ⚠️ `ltgd` `pageSize` 参数不限制返回明细体量（仍返回全历史 8–18 日期/80–180 条），须读全文或落盘解析。
    5. `tdx_quotes.BelongHGTAG` 先判定港股通资格，非港股通直接 N/A（C2 不适用）。
- **已知限制（列后续接入项）**：tdx 当前工具未直接暴露 `FINVALUE(255)/FINVALUE(239)` 接口，C1 在 ltgd 前十大截断下为下界估算；权威值需 FINVALUE 接口终核。
- **产物**：`deliverables/taobo-daily/daily-screening-2026-08-06-v2.md`（取数通路验证版，消除原 8 只假缺失）。
- **核心条件声明**：C1/C2 锚定定义未改；本次仅固化取数通路与缺失处置，落实 v1.4.4 第6点的「缺失分三类回退」纪律。

### v1.5 — 2026-08-07（RPS 真实取数落地 · 用户指令"落地，然后更新skill的过滤层，把陶博士的RPS的筛选条件实装"）
- **类型**：核心层执行实装（消除 v1.4 的 RPS 代理）+ 新增本地数据源
- **背景**：v1.4 因 neodata/tdx 无 RPS 字段，C4-C6 以「一年新高+阶段涨幅+强弱表现」代理执行（标注「RPS代理」）。2026-08-07 用户本地通达信 EXTDATA 取数链路打通（数据刷新至 2026-08-07），RPS 可真实读取，故将 C4-C6 **实装**为真实 RPS 判定。
- **新增文件**：`references/rps_lookup.py`（自包含 RPS 桥接，仅标准库，零接口费）
  - 读取 `D:\Sofeware\TongDaXin\T0002\extdata\extdata_{sid}.{dat,idx}`（已逆向：`.idx` 29字节/条、末4字节=每股票记录数；`.dat` 变长紧打包，Σ(count)×12==.dat大小，全部 8 个 ID 精确吻合）
  - 提供 `get_stock_rps(code)`（RPS250/120/50/20/10）、`get_sector_rps(code)`（板块 RPS5/10/20）、`eval_c4c5c6(rps)`（按锚定定义判 C4/C5/C6）
  - CLI：`python references/rps_lookup.py <代码>` / `--sector <板块代码>` / `--json`
- **C4-C6 实装（锚定定义不变）**：
  - C4（RPS优先一切）：RPS120≥90 或 RPS250≥90（基本面优秀品种可放宽≥85）
  - C5（一线红）：RPS50/120/250 任一≥90
  - C6（三线红·小黄人）：RPS50&RPS120&RPS250 均≥90
- **双RPS 板块共振（用户2026-08-07 强调·新增执行层）**：个股通过 C4-C6 后额外取所属板块 RPS；个股三线红 **且** 板块 RPS20(ID13)≥90 → 「双RPS共振·强」；个股强但板块弱 → 「个股独立行情·板块逆风」，降权不否决。
- **SKILL.md 变更**：
  - Step 3 第2层「RPS 代理说明」整段替换为「RPS 真实取数」：调用 `rps_lookup.py` 取真实 RPS，按 C4-C6 阈值实装
  - 漏斗表「第2层-强度代理」→「第2层-强度(RPS真实)」
  - 数据获取 B 节新增 RPS 本地数据源条目；「RPS 限制」注记改为「RPS 真实取数（方案A）」
  - Step 2 数据源限制注记补充"RPS 现已改由通达信 EXTDATA 本地读取"
- **taobo-core-conditions.md 变更**：第2层「执行代理注记」改为「真实取数注记」，附 ID 映射（RPS250=1/RPS120=2/RPS50=3/RPS20=4/RPS10=5；板块 RPS5=11/RPS10=12/RPS20=13）+ 双RPS 共振说明 + 数据时效声明
- **用户审批**：用户 2026-08-07 明确指令「落地，然后更新skill的过滤层，把陶博士的RPS的筛选条件实装」→ 视为批准
- **核心条件声明**：C1-C23 锚定定义**全部未改**；C4-C6 仅将执行层由"代理"升级为"真实 RPS 取数"，锚定阈值（≥90）与逻辑（或/且）完全保留。此为能力补全，非条件迭代。
- **已知限制**：① RPS 依赖通达信盘后「扩展数据计算」刷新，筛选日须与 extdata 刷新日对齐（当前 2026-08-07）；② 某代码返回「无数据」= 未刷新/不在索引 → 标「RPS未取·待核」，不自动通过；③ 个股→板块代码自动映射需 `blocknew/*.blk`，当前由种子池已知板块代码或手动提供。

---

## v1.6 (2026-08-07 · 双RPS 板块自动映射落地)

- **问题**：v1.5 双RPS 共振执行层已就位，但「个股→所属板块代码」仍需手动提供/种子池维护，未真正自动化。
- **根因调研**：通达信板块成员并非存于 `blocknew/*.blk`（仅 39 个用户自定义板块，且为 7字节/股格式），也非 `block_gn.dat/zs.dat/fg.dat` 的 Registry 二进制（板块码为短码 88/163… 非 880xxx，与 extdata 板块RPS 代码体系不一致）。经全量扫描 `hq_cache` 下所有 .dat，**`infoharbor_block.dat` 为文本(CSV)格式、含 880xxx 板块码 + 成员清单**，与 extdata 板块RPS(ID11/12/13)的 880xxx 代码体系完全对齐。
- **实现**：`rps_lookup.py` 新增 `build_block_map()`（解析 `infoharbor_block.dat` 文本：块头 `#GN_中文名,短码,880xxx,创建日,更新日,,`，成员 `X#YYYYYY` 逗号分隔）+ `get_stock_boards(code)` + `get_double_rps(code)`。结果：**269 个行业/概念板块、6022 条个股→板块映射，全自动**。
- **验证（真实数据）**：
  - `600999` 示例有色股 → 自动映射 稀缺资源(880505)/国防军工(880507)/稀土永磁(880535)/MLCC概念(880588)/氢能源(880705)/新材料(880731)；稀缺资源 RPS20=90.2≥90 → 判定「板块强·个股弱」。
  - `603259` 药明康德 → 个股三线红 + 自动识别 CXO概念(880729) RPS20=94.4≥90 → 判定「双RPS共振·强」✅（板块归属完全正确）。
- **CLI**：`python references/rps_lookup.py --double <个股代码>` 一键输出双RPS共振判定。
- **文档同步**：SKILL.md（双RPS 段 + RPS 真实取数注记）、taobo-core-conditions.md（第2层双RPS 注记）改为「自动映射，无需手动板块代码」；删除 v1.5 已知限制③的「映射未实现」表述。
- **用户审批**：用户 2026-08-07 指令「把映射补上」→ 视为批准将双RPS 自动映射实装。
- **核心条件声明**：C1-C23 锚定定义**全部未改**；本次仅补全双RPS 的「板块自动归属」能力，属工程落地，非条件迭代。

---

## v1.7 (2026-08-08 · 本地RPS 取数能力归属确权 · 单一真源)

- **类型**：能力归属确权（skill 产品化治理，非条件迭代）
- **背景**：用户指令「把 realrps-backtest-pipeline 中取本地 RPS 的流程交给陶博选股 skill」。治理前现状核实：
  1. `rps_lookup.py` 全仓仅 **一份**，位于 `taobo-review-iteration/references/`；realrps-backtest-pipeline 目录**无任何 .py**，仅 SKILL.md 文档引用。
  2. `gen_signals_realrps_2023_2025.py`（位于 `deliverables/taobo-daily/`）第 21 行 `sys.path.insert(0, r"<taobo-review-iteration skill>/references")` + 第 24 行 `import rps_lookup as R`，已走委托复用，无重复副本。
- **内容（确权 + 文档归属补齐）**：
  - 确权 `references/rps_lookup.py`（RPS 取数 / ID 映射 / 双RPS / `asof` 无前视）为「**本 skill 拥有的核心能力**」，单一真源。
  - realrps-backtest-pipeline 定位：**只消费、不复制**。其 SKILL.md「关键事实」原 4 条 RPS 契约改为**委托声明**（本地 RPS 由陶博士选股 skill 拥有，本流水线复用，不另立副本；完整契约见 taobo SKILL.md Step 3 第2层 + rps_lookup.py 模块 docstring）；依赖项标注 `rps_lookup.py` 归属。
  - 陶博士 SKILL.md Step 3 第2层新增「〔能力归属〕本地RPS 真实取数为本 skill 拥有的核心能力 · 单一真源、禁止在其它 skill 另立副本」；本地化工具链索引 `rps_lookup.py` 行标「**（本 skill 拥有）**」；索引尾部 ⚠️ 声明「本地 RPS 真实取数为本 skill 拥有的核心能力，禁止在其它 skill 复制或重写 RPS 读取逻辑」。
  - **契约变更纪律**：任何 RPS 契约变更（ID 映射 / C4-C6 阈值 / 双RPS 映射 / `asof` 逻辑）只在 taobo SKILL.md Step 3 第2层 + `rps_lookup.py` 内维护，禁止在其它 skill 复制或重写。
- **验证**：① `rps_lookup.py` 全仓唯一（taobo skill references/）；② import 通路正常（600999 实测返回 RPS250/120/50/20/10 全序列）；③ realrps skill 目录无 .py，仅 SKILL.md 引用，无复制风险。
- **用户审批**：用户 2026-08-08 指令「把 realrps-backtest-pipeline 中取本地 RPS 的流程交给陶博选股 skill」→ 视为批准确权。
- **核心条件声明**：C1-C23 锚定定义**全部未改**；本次为工程治理（能力归属单一真源），非条件迭代。

---

## v1.8 (2026-08-08 · 第8层描述补全 · 各档具体加分条件入 skill)

- **类型**：文档增强（非条件迭代，纯文本补全）
- **背景**：用户指令「把各档具体的加分条件的陈述加入到 skill 描述里」。原 SKILL.md 第8层 blockquote 仅概括「硬门槛 = 盈利TTM不亏 + ROE≥10% + 审美分档」，未列具体加分因子，需与代码 `fundamental_filter.py` `evaluate()`（quality_floor 预设）对齐、写进 skill 文本。
- **内容（SKILL.md 第8层 blockquote 扩充）**：
  - **① 硬门槛显式列出**：F0-1 盈利TTM不亏（归母净利TTM>0，亏损一票否决）+ F2-1 ROE(TTM)≥10%（`roe_th=0.10`；strict 预设≥15%）；任一不过→直接 C 档。
  - **② 加分项展开为 12 条具体因子清单**（每项 +1 分，满分 12）：F0-1b扣非为正 / F0-2扣非正增长 / F0-3低商誉(<30%) / F0-4现金流好(≥50%) / F1-1单季扣非高增(≥25%,C19) / F1-2年报复合高(≥20%,C20) / F1-3营收高增(≥15%) / F2-1b ROE更高(≥15%) / F2-2毛利率(≥30%) / F3-1基金≥3%(C1) / F3-2北向≥3000万(C2) / F4-2 PEG≤1(C21)；每条附阈值与锚定条件编号。
  - **③ 分档定义改写**：A档（审美分≥5）硬通过必留 / B档（3-4）仅评分项权重×0.5 / **C档（硬门槛不过 或 审美分≤2）硬否决**——明确 C 档含「硬门槛不过」情形。
- **一致性**：与 `fundamental_filter.py` `evaluate()` quality_floor 预设（L120–344）逻辑完全一致；strict 预设对照（六层全硬）仍保留在 docstring 与 PRESETS。
- **用户审批**：用户 2026-08-08 指令「把各档具体的加分条件的陈述加入到 skill 描述里」→ 视为批准。
- **核心条件声明**：C1-C23 锚定定义**全部未改**；本次为描述补全（代码既有 quality_floor 因子的文本化），非条件迭代。

---

## v1.9 (2026-08-10 · 重点观察池规则化 + 候选排序规则入 skill)

- **类型**：输出规范更新（非核心条件迭代）
- **背景**：用户 2026-08-10 两条指令：①「不要只固定 top5 用来做重点观察，设置一个规则，满足规则的即列入观察，不管符合条件的标的有多少」；②「把今天写进长期记忆的新规则更新进 skill」。
- **内容（SKILL.md Step 5 + daily-screening-template.md 同步）**：
  1. **废除「重点关注标的 TOP 5」固定数量概念**，改为「**重点观察池（规则达标制 · 达标即入，不设数量上限）**」。
  2. **三通道入选规则**（满足任一即入，多通道兼中按 A>B>C 标注单通道）：
     - 通道A 质量顶级：第8层加分 ≥ 10/12
     - 通道B 质量+三线红：加分 ≥ 9 且 C6（RPS50/120/250 全≥90）
     - 通道C 质量+板块共振：加分 ≥ 8 且 双RPS（所属板块 RPS20≥90）
  3. **候选排序规则**：加分降序 → 双RPS共振(有排前) → C6三线红(有排前) → RPS250 → RPS120。
  4. 模板「重点观察池」章节示例表改为通道标注格式（#/通道/代码/名称/加分/C6/双RPS/RPS250/RPS120/板块/历史候选）；操作提示保留「建议观察回踩目标均线（10/20/50/120 日线）」。
- **首日执行（2026-08-10）**：23 只达标（A=14 / B=2 / C=7），占 A+B 76 只的 30.3%；实现参考 `deliverables/taobo-daily/generate_daily_report_2026_08_10.py` 的 `watch_channel()`。
- **用户审批**：用户 2026-08-10 拍板（指令①直接批复；指令②要求入 skill）。
- **核心条件声明**：C1-C23 锚定定义**全部未改**；本次为输出规范/排序呈现层更新（观察池口径与排序键），非条件迭代。第8层 quality_floor 硬门槛/加分阈值（v1.8 所载）未动。

---

## v2.0 (2026-08-12 · 基底计数维度入排序次级键 · 硬过滤证伪)

- **类型**：输出规范更新（候选排序键扩展，非核心条件迭代）
- **背景**：用户 2026-08-12 指令「跑交叉网格确认组合层面贡献」→ 接受建议「基底计数作排序次级信息，不作硬过滤」→ 同步进 skill。
- **回测证据（两条齐全）**：
  1. **事件研究（支持计数有预测力）**：候选池 546 只 · 14639 突破事件 · 持有 60 交易日——第1-2基底突破 胜率 48.7% / 平均收益 +5.31% vs 第3+基底 37.7% / +0.31%；第4基底转负（-3.16%）、第6+基底 -13.82%，基底数单调恶化（完全吻合基底.txt"第3/4个基底更易失败"）。
  2. **60 组合交叉网格（证伪硬过滤）**：剔除信号日基底≥4 的信号（68 条/4229=1.6%）→ 仅 11/30 组合正贡献、19/30 负贡献；实盘骨架 8-6-4·G2·R72 +1025.9%→+740.2%（**-285.7pp**）、8-6-4·GA·R72 -319.2pp。**根因**：4+ 基底信号 81% 集中 2025 牛市（动态净值复利 + R7-2 腾位换强下恰是收益主力），硬过滤=误杀牛市行情。事件研究（静态单笔）与组合层面（动态机制）结论相反——再次验证 #10 铁律。
- **内容（SKILL.md 候选排序规则同步）**：候选排序键末尾追加**基底计数（完全同分时基底少者排前）**；明确禁止作硬过滤/降级。
- **实现资产**：`trading-dashboard/scripts/base_count.py`（状态机）+ `deliverables/taobo-daily/base-filter-grid-compare-2016-2025.html`（60组合报告）+ `add_base_count_to_signals.py` / `run_grid_base.py`。
- **核心条件声明**：C1-C23 锚定定义**全部未改**；本次为排序呈现层扩展（末级同分微调），非条件迭代。
